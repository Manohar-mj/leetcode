class Solution:
    def removeStars(self, s: str) -> str:
        out = []
        for c in s:
            if c == "*":
                out.pop()
            else:
                out.append(c)
        return "".join(out)
